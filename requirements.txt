aiogram==3.13.1
requests==2.32.3
aiosqlite==0.20.0
# Алго-торгівля не потребує важких залежностей: клієнт Binance — на requests + hmac
# (stdlib), бектест/метрики — чистий Python. ccxt/pandas/numpy НЕ потрібні.
# --- Опційні (вмикаються лише якщо встановлені + задані ключі) ---
redis==5.0.8          # розподілений кеш (потрібен REDIS_URL)
sentry-sdk==2.14.0    # моніторинг помилок (потрібен SENTRY_DSN)
pillow==10.4.0        # лише для застарілого рендера карток (.bak_refactor)
