aiogram==3.13.1
requests==2.32.3
pillow==10.4.0
aiosqlite==0.20.0
# --- Опційні (фічі вмикаються лише якщо встановлені + задані ключі) ---
openai==1.51.0        # AI-помічник (потрібен OPENAI_API_KEY)
redis==5.0.8          # розподілений кеш (потрібен REDIS_URL)
sentry-sdk==2.14.0    # моніторинг помилок (потрібен SENTRY_DSN)
