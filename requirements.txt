aiogram==3.13.1
requests==2.32.3
aiosqlite==0.20.0
# Алго-торгівля без важких залежностей: клієнт Binance — на requests + hmac
# (stdlib), бектест/метрики — чистий Python. ccxt/pandas/numpy НЕ потрібні.
# --- Опційне ---
sentry-sdk==2.14.0    # моніторинг помилок (вмикається лише за SENTRY_DSN у .env)
